source 'https://rubygems.org'

gem 'scraperwiki-morph'

# These specific gems and versions are those installed on ScraperWiki "classic" as of 17 Aug 2013
# Note also that the ruby version is set by .ruby-version in this directory

gem "mechanize", "2.1"
gem "json", "1.6.5"
gem "rfgraph", "0.3"

# Force mechanize dependencies to lower versions
gem "domain_name", "0.5.2"
gem "net-http-digest_auth", "1.2"
gem "net-http-persistent", "2.4"
gem "nokogiri", "1.5.0"
gem "webrobots", "0.0.13"

# Force domain_name dependencies to lower versions
gem "unf", "0.0.4"

# Force unf dependencies to lower versions
gem "unf_ext", "0.0.4"

# End of gems for ScraperWiki "classic"
