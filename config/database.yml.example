development: &development
  adapter: mysql2
  encoding: utf8mb3 # TODO: update to utf8mb4
  collation: utf8mb3_unicode_ci # TODO Update to utf8mb4_general_ci (5.7 compatible) or default in 8.0
  username: <%= ENV.fetch("MYSQL_USER", "scraping").inspect %>
  password: <%= ENV.fetch("MYSQL_ROOT_PASSWORD", "").inspect %>
  host: <%= ENV.fetch("MYSQL_HOST", "localhost").inspect %>
  pool: 5
  database: scraping_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *development
  database: scraping_test

production:
  adapter: mysql2
  encoding: utf8mb3 # TODO: update to utf8mb4
  collation: utf8mb3_unicode_ci # TODO Update to utf8mb4_general_ci (5.7 compatible) or default in 8.0
  database: scraping_production
  username: scraping
  password: "REPLACE WITH A SECURE PASSWORD"
  # This needs to be high to handle the concurrency in sidekiq
  pool: 25
