gem:
  # Add your `gem` command parameters here:
  #
  exclude:
  - rdoc
  # doc: true
  # workers: 5
dsl:
  # Add your `dsl` command parameters here:
  #
  # exclude:
  # - SomeGeneratorName
  # workers: 5
annotations:
  typed_overrides:
    sidekiq: "ignore"
