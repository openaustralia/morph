-provisioning/roles/morph-app/tasks/main.yml
  
   -config/deploy.rb


     -before_script
     -env{SECRET_KEY_BASE=859384 RUN_SLOW_TESTS=true
     -before_install:
  - gem install bundler:1.17.3
     -language: ruby
rvm: {en-java-client
