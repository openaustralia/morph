# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# ignore yarn output
/node_modules

# Precompiled assets
/public/assets

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and most tempfiles.
/log/*.log
/tmp/cache
/tmp/miniprofiler
/tmp/sessions
/tmp/sockets
/tmp/restart.txt

# Ignore the PID tempfiles but not the folder that holds them.
/tmp/pids/*.pid

# Temp files
*.bak

# Used by direnv / dotenv library to load environment variables.
/.env*

# Project specific cache
/db/scrapers

# Ignore Vagrant artifacts
.vagrant
.vagrant-plugins-installed
vagrant_ansible_inventory_local
vagrant_ansible_inventory_production
vagrant_ansible_inventory_production2

# Ignore database config (copy and adjust example)
config/database.yml

# Private key for morph GitHub application
config/morph-github-app.private-key.pem

# test coverage reports
coverage

# Backups
db/backups

# local override of docker compose config
/docker-compose.override.yml

# Provisioning artifacts
docker_images/morph-mitmdump/mitmproxy/mitmproxy-dhparam.pem
playbook.retry
provisioning/.roles-installed

# Ignore roles (re)installed using provisioning/requirements.yml
provisioning/roles/geerlingguy.java
provisioning/roles/azavea.elasticsearch
provisioning/roles/azavea.java
provisioning/roles/newrelic.newrelic-infra
provisioning/roles/nickhammond.logrotate
provisioning/roles/geerlingguy.elasticsearch/
provisioning/roles/ansible-yarn

# Ignore self signed keys (generated by infrastructure util)
provisioning/roles/morph-app/files/ssl/dev.morph.io.key
provisioning/roles/morph-app/files/ssl/dev.morph.io.pem

# Python virtualenv
.venv/

# IDE and editor files
/.aider*
/.idea
/.vscode*

# Ignore vim files:
*~
*.swp
*.swo

# Ignore patch files
*.patch
*.rej


# Ignore Byebug command history file.
.byebug_history

## Specific to RubyMotion:
.dat*
.repl_history
build/
*.bridgesupport
build-iPhoneOS/
build-iPhoneSimulator/

# Bundler
/vendor/bundle/
/.bundle/
/lib/bundler/man/

## Documentation cache and generated files:
/.yardoc/
/_yardoc/
/doc/
/rdoc/

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Used by RuboCop. Remote config files pulled in from inherit_from directive.
.rubocop-https?--*

# rspec reports
/.rspec_status

