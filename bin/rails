#!/usr/bin/env ruby
# load File.expand_path("spring", __dir__) # disable spring as it was causing Rails::Application is abstract, you cannot instantiate it directly in railties gem
APP_PATH = File.expand_path('../config/application', __dir__)
require_relative "../config/boot"
require "rails/commands"
