#!/usr/bin/env ruby
begin
  # disable spring as it was causing Rails::Application is abstract, you cannot instantiate it directly in railties gem
  # load File.expand_path("../spring", __FILE__)
rescue LoadError
end
require 'bundler/setup'
load Gem.bin_path('rspec-core', 'rspec')
