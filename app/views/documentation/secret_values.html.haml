:markdown
  # Secret values

  Scraper code on morph.io is public and visible to anyone. So, how can you
  use an authenticated API, say the Twitter API, in a scraper without letting
  everyone know your authentication credentials?

  This is where secret values come in. These are environment variables you
  can set. Only you and your scraper can see them.

  ### Setting secret values

  Let's say you have a secret value `sdf8sdbw9Hihf` you want to use in your scraper.
  We will now assign that value to an environment variable.
  We'll call it `MORPH_MYSECRET`. The name needs to
  start with `MORPH_`.

  1. Go to your scraper and click the "Settings" button
  2. Click "Add variable" and fill in the environment variable name and value
  3. Click "Update"

  Before you clicked "Update" it should have looked something like

.well
  .row
    .col-sm-3
      %label Name
    .col-sm-8
      %label Value

  .row
    .col-sm-3
      = text_field_tag :name, "MORPH_MYSECRET", disabled: true, class: "form-control"
    .col-sm-8
      = text_field_tag :value, "sdf8sdbw9Hihf", disabled: true, class: "form-control"

:markdown
  You can of course set as many different secret values as you want. You just
  need to click "Add variable" again and give each secret value its own
  unique name.


:markdown
  ### Reading secret values in your scraper

%div
  %ul.nav.nav-tabs(role="tablist")
    %li.active(role="presentation")
      = link_to '#ruby', 'aria-controls' => "ruby", role: "tab", 'data-toggle' => "tab" do
        = language_name_with_icon(:ruby, size: "20x20")
    %li(role="presentation")
      = link_to '#php', 'aria-controls' => "php", role: "tab", 'data-toggle' => "tab" do
        = language_name_with_icon(:php, size: "20x20")
    %li(role="presentation")
      = link_to '#perl', 'aria-controls' => "perl", role: "tab", 'data-toggle' => "tab" do
        = language_name_with_icon(:perl, size: "20x20")
    %li(role="presentation")
      = link_to '#python', 'aria-controls' => "python", role: "tab", 'data-toggle' => "tab" do
        = language_name_with_icon(:python, size: "20x20")
    %li(role="presentation")
      = link_to '#nodejs', 'aria-controls' => "nodejs", role: "tab", 'data-toggle' => "tab" do
        = language_name_with_icon(:nodejs, size: "20x20")

  .tab-content
    .tab-pane.active#ruby(role="tabpanel")
      = render "documentation/reading_secret_values/ruby"
    .tab-pane#php(role="tabpanel")
      = render "documentation/reading_secret_values/php"
    .tab-pane#perl(role="tabpanel")
      = render "documentation/reading_secret_values/perl"
    .tab-pane#python(role="tabpanel")
      = render "documentation/reading_secret_values/python"
    .tab-pane#nodejs(role="tabpanel")
      = render "documentation/reading_secret_values/nodejs"
