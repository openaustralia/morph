- content_for :title, "Settings for #{@scraper.full_name}"
%h1
  = link_to @scraper.owner.to_param, @scraper.owner
  \/
  = link_to @scraper.name, @scraper

%p= link_to "Back to the scraper", @scraper

= render "settings"
= render "danger"
