.container
  .row.text-center
    %h2 Thank you
    .lead
      %p
        The continued support of these wonderful people keeps morph.io free for everyone and makes it even more awesome.
      - unless user_signed_in? && current_user.supporter?
        %p Join them by #{link_to "becoming a supporter", new_supporter_path}.

  .row
    - Plan.all_plans.each do |plan|
      - unless Owner.supporters(plan).empty?
        .col-xs-12
          %h2
            = link_to plan.name.pluralize, new_supporter_path
          = render Owner.supporters(plan)
