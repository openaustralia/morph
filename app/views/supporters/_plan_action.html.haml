= button_tag "Signup", class: "btn btn-default btn-lg",
                       data: {key: Rails.configuration.stripe[:publishable_key],
                              description: description,
                              amount: amount,
                              email: current_user.email,
                              plan_id: plan_id}
